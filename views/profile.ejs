<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com"> 
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> 
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;0,1000;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900;1,1000&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/static/stylesheets/main.css">
    <link rel="stylesheet" href="/static/stylesheets/mobile.css">
    <title>Profile: <%= name %></title>
</head>
<body>
    <div class="main-container">
        <%- include('./nav'); -%>
        <div class="post-container">
            <% blogEntry.forEach(details => { %>
                <% if (details.name == name) { %>
                <ul class="blogEntries">
                    <li class="entry">
                        <h3 class="title"><%= details.title %><span> by </span><span class="userLink profile"><%= details.name %></span><span> <%= details.date %></span></h3>
                        <p class="body"><%= details.content %></p>
                    </li>
                </ul>
                <% }; %>
            <% }) %>
        </div>
    </div>
    
</body>
</html> 